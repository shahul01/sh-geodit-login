import { FC, useEffect, useState } from 'react';

interface IFormProps {
};

const Form: FC<IFormProps> = (props) => {

  return (
    <div className='form'>
      Login | Register
    </div>
  )
};

export default Form;
